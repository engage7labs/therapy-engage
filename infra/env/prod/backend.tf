# Backend configuration for PROD environment
# For local development, comment out the backend block to use local state
# For production use, configure remote state storage

# terraform {
#   backend "azurerm" {
#     # Configure these values via environment variables or terraform init:
#     # storage_account_name = "your-terraform-state-storage"
#     # container_name       = "tfstate" 
#     # resource_group_name  = "your-terraform-state-rg"
#     # key                  = "prod.tfstate"
#   }
# }
